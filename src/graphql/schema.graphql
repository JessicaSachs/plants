### This file was generated by Nexus Schema
### Do not make changes to this file directly


type App {
  """All the planters in the garden"""
  planters(planterId: String): [Planter!]!
}

"""
A date-time string at UTC, such as 2007-12-03T10:15:30Z, compliant with the `date-time` format outlined in section 5.6 of the RFC 3339 profile of the ISO 8601 standard for representation of dates and times using the Gregorian calendar.
"""
scalar DateTime

"""Represents a feeding"""
type Feeding {
  feedAmount: Int!
  id: ID!
  interval: Int
  nextDate: String!
  overdue: Boolean!
  shouldClean: Boolean!
  times: Int
}

"""
The `JSON` scalar type represents JSON values as specified by [ECMA-404](http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf).
"""
scalar JSON @specifiedBy(url: "http://www.ecma-international.org/publications/files/ECMA-ST/ECMA-404.pdf")

type Mutation {
  """Feed the planter on a specific date"""
  feedPlanter(input: PlanterFeedInput): [Planter!]!
}

"""Represents a planter"""
type Planter {
  id: ID!
  lastFeedDate: String!
  model: PlanterModels!
  name: String!
  nextFeeding: Feeding!
  schedule: [Feeding!]!
  size: PlanterSizes!
  startDate: String!
}

input PlanterFeedInput {
  date: String
  planterId: String
}

enum PlanterModels {
  bountyelite
  harvest360
}

enum PlanterSizes {
  nine
  six
}

"""Root query"""
type Query {
  app: App!
}
